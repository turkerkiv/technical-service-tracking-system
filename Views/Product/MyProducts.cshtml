@model List<CustomerProductViewModel>

<div class="w-full bg-slate-700 h-full px-36 py-10 flex flex-col items-start space-y-10 justify-start">
    <h1 class="font-semibold text-5xl text-white">Your Products</h1>
    <table class="min-w-full border-collapse bg-slate-600 shadow-md rounded-lg overflow-hidden text-gray-300">
        <thead>
            <tr class="bg-slate-800 text-left text-sm font-semibold text-gray-300 border-b  border-slate-700">
                <th class="py-2 px-4 ">@Html.DisplayNameFor(c =>
                    c[0].ProductName)</th>
                <th class="py-2 px-4 ">@Html.DisplayNameFor(c =>
                    c[0].SerialNumber)</th>
                <th class="py-2 px-4">@Html.DisplayNameFor(c =>
                    c[0].WarrantyStartDate)</th>
                <th class="py-2 px-4">@Html.DisplayNameFor(c =>
                    c[0].WarrantyEndDate)</th>
                <th class="py-2 px-4 ">@Html.DisplayNameFor(c =>
                    c[0].HasWarranty)</th>
                <th class="py-2 px-4 ">Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var product in Model)
            {
            <tr class="hover:bg-gray-700 border-b  border-slate-700">
                <td class="py-3 px-4 font-semibold text-gray-200">@product.ProductName</td>
                <td class="py-3 px-4">@product.SerialNumber</td>
                <td class="py-3 px-4">@product.WarrantyStartDate.ToString("MM/dd/yyyy")
                </td>
                <td class="py-3 px-4 ">@product.WarrantyEndDate.ToString("MM/dd/yyyy")</td>
                <td class="py-3 px-4 ">
                    <span class="inline-block px-2 py-1 rounded-lg text-xs font-medium
                                @(product.HasWarranty ? "bg-green-200 text-green-800"
                        : "bg-red-200 text-red-800" )">
                        @(product.HasWarranty ? "In Warranty" : "Out of Warranty")
                    </span>
                </td>
                <td class="py-3 px-4">
                    <a asp-controller="Request" asp-action="CreateRequest" asp-route-productId="@product.Id"
                        class="inline-block px-4 py-2 text-white bg-green-700 rounded hover:bg-green-800 focus:outline-none focus:ring-2 focus:ring-green-500">
                        Create Service Request
                    </a>
                </td>
            </tr>
            }
        </tbody>
    </table>
</div>

<!-- 
<form action="" class="flex flex-col items-end bg-gray-700 p-4">
    <div class="mb-3 w-full flex flex-col  space-y-2">
        <label for="selectedProduct" class="me-2 text-xl">Select your product</label>
        <select name="selectedProduct" id="selectedProduct"
            class="ring-1 ring-gray-400 rounded px-4 py-2 bg-transparent outline-none focus:border-none hover:bg-slate-700 focus:ring-2 focus:ring-gray-400 focus:bg-slate-700 transition duration-100">
            <option value="">Select an option</option>
            @foreach (var product in Model)
            {
            <option value="@product.Id">@product.ProductName</option>
            }
            <option value="">Add a product... ↗</option>
        </select>
    </div>
    <div class="mb-3 flex flex-col w-full space-y-2">
        <label asp-for="FaultDetails" class="me-2 text-xl"></label>
        <textarea asp-for="FaultDetails"
            class="ring-1 ring-gray-400 rounded px-4 py-2 bg-transparent outline-none focus:border-none hover:bg-slate-700 focus:ring-2 focus:ring-gray-400 focus:bg-slate-700 transition duration-100"
            rows="5" cols="35" style="resize: none;"></textarea>
        <span asp-validation-for="FaultDetails"
            class="text-red-400 block mt-1 text-end text-xs font-medium"></span>
    </div>
    <div>
        @Html.Partial("_Button", "Submit")
    </div>
</form> -->